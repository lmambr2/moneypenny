# Rust bot workspace (WIP)

Strangler rewrite of the Node bot process. See [docs/rust-rewrite.md](../docs/rust-rewrite.md).

```bash
cargo test --workspace
cargo test -p mp-audio
cargo run -p moneypenny
```
