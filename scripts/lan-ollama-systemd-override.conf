[Service]
Environment="OLLAMA_HOST=0.0.0.0"
Environment="OLLAMA_KEEP_ALIVE=6h"
# One resident LLM. 12B+31B on a 32 GB Radeon plus Whisper OOMs host RAM
# via amdgpu HMM even when VRAM "looks" like it should fit.
Environment="OLLAMA_MAX_LOADED_MODELS=1"
